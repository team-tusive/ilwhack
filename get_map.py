import urllib2

def fromFile(filename):
	'''Takes csv file of incident data (as generated by scrape_police_data)'''
	pass


def fromDictionaries(*args):
	'''Takes dictionaries of Incidents'''
	pass


def fromIncidents(*args):
	'''Tales Incidents'''
	pass

def _requestMap(size=(500,400),*args):
	'''Takes (sizex,sizey) and (lat,long) tuples. Size defaults to 500 by 400'''
	paramstring = 'http://maps.googleapis.com/maps/api/staticmap?sensor=false&size=%s&visible=' %(str(size)[1:-1])
	delimiter = '%7C'
	paramstring += delimiter.join([','.join(arg) for arg in args])
	print paramstring
	img = urllib2.urlopen(paramstring).read()
	return img
